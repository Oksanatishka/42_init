# What file has to be modified to make it permanent?
 
/etc/hostname --> insert newname in the file

# The hostname saved in this file (/etc/hostname) will be preserved on system reboot (and will be set using the same script we used hostname.sh).
