# What file contains the RSA keys of systems that are authorized to connect via SSH?
#
# Run ssh-keygen(then enter a path /root/.ssh/id_rsa) on your machine, hit enter when asked for a password. 
# OR cat ~/.ssh/id_rsa

cat ~/.ssh/authorized_keys
 